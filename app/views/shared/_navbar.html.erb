<nav class="bg-white shadow-md px-4 py-3">
  <div class="max-w-7xl mx-auto flex justify-between items-center">
    <div class="w-1/3"></div> <!-- left spacer -->

    <div class="w-1/3 flex justify-center">
      <span class="text-2xl font-bold tracking-wide text-gray-800">GPA TM</span>
    </div>

    <div class="w-1/3 flex justify-end space-x-4">
      <% if user_signed_in? %>
<%= link_to 'Sign Out', destroy_user_session_path,
  data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
  
      <% else %>
        <%= link_to "Teacher Login", new_teacher_session_path, class: "text-gray-700 hover:text-blue-600" %>
        <%= link_to "Student Login", new_student_session_path, class: "text-gray-700 hover:text-blue-600" %>
        <%= link_to "Admin Login", new_admin_session_path, class: "text-gray-700 hover:text-blue-600" %>
      <% end %>
    </div>
  </div>
</nav>
