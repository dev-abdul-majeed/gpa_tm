<nav class="bg-gradient-to-r from-slate-50 to-blue-50 border-b-2 border-slate-200 shadow-lg px-4 py-4">
  <div class="max-w-7xl mx-auto flex justify-between items-center">
    <div class="w-1/3"></div> <!-- left spacer -->

    <div class="w-1/3 flex justify-center">
      <span class="text-3xl font-bold tracking-wide bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">GPA TM</span>
    </div>

    <div class="w-1/3 flex justify-end space-x-3">
      <% if user_signed_in? %>
        <%= link_to destroy_user_session_path,
          data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'},
          class: "bg-gradient-to-r from-red-500 to-pink-500 text-white px-4 py-2 rounded-lg font-medium shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 flex items-center gap-2" do %>
          <i class="fa-solid fa-sign-out-alt"></i>
          Sign Out
        <% end %>
      <% else %>
        <%= link_to new_teacher_session_path, 
            class: "bg-gradient-to-r from-emerald-500 to-teal-500 text-white px-4 py-2 rounded-lg font-medium shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 flex items-center gap-2" do %>
          <i class="fa-solid fa-chalkboard-user"></i>
          <p>Teacher</p>
        <% end %>

        <%= link_to new_student_session_path, 
          class: "bg-gradient-to-r from-orange-500 to-amber-500 text-white px-4 py-2 rounded-lg font-medium shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 flex items-center gap-2" do %>
          <i class="fa-solid fa-graduation-cap"></i>
          <p>Student</p>
        <% end %>
        <%= link_to new_admin_session_path, 
          class: "bg-gradient-to-r from-indigo-500 to-purple-500 text-white px-4 py-2 rounded-lg font-medium shadow-md hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 flex items-center gap-2" do %>
          <i class="fa-solid fa-users-gear"></i>
          Admin
        <% end %>
      <% end %>
    </div>
  </div>
</nav>