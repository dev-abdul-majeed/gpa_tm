<div class="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50">
  <div class="container mx-auto px-4 py-8">
    <!-- Header -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center gap-4 mb-4">
        <div class="w-12 h-12 bg-gradient-to-r from-emerald-500 to-teal-500 rounded-lg flex items-center justify-center">
          <i class="fas fa-clipboard-check text-white text-xl"></i>
        </div>
        <h1 class="text-3xl font-bold text-slate-800">Create New Assignment</h1>
      </div>
      <p class="text-slate-600">Step 3: Configure your <%= @assignment.assignment_type&.upcase %> assignment for <span class="font-semibold text-emerald-600"><%= @course.name %></span></p>
    </div>


    <!-- Assignment Form -->
    <div class="max-w-4xl mx-auto">
      <%= render 'form', assignment: @assignment, course: @course %>
    </div>
  </div>
</div>
